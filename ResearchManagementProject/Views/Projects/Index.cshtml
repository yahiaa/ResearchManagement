@model IEnumerable<ResearchManagementProject.Project>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Prj_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prj_Desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prj_StageId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prj_LevelId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Prj_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prj_Desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prj_StageId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prj_LevelId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
